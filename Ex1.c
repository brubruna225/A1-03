#include <stdio.h>

    float valueBem, valueDepre, valueBemDepre, DepreAcumu
    int periodo;

int main() {
    printf("Informe valor a ser depreciado: ");
    scanf("%f", &valueBem);

    printf("Informe em anos a ser depreciado: ");
    scanf("%i", &valueBemDepre);
    
    fffff

    return 0;
}